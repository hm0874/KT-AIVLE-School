def 최대_전투력_계산(N, 전투력):
    # 용병들의 전투력을 내림차순으로 정렬합니다.
    전투력.sort(reverse=True)
    
    # 최대 전투력의 합을 계산합니다.
    최대_전투력_합 = 0
    for i in range(N):
        현재_전투력_합 = 전투력[i] * (i + 1)
        if 현재_전투력_합 > 최대_전투력_합:
            최대_전투력_합 = 현재_전투력_합
    
    return 최대_전투력_합

# 입력 예시
N = int(input())
전투력 = list(map(int, input().split()))

# 출력
print(최대_전투력_계산(N, 전투력))
